{% extends 'form_div_layout.html.twig' %}

{% block file_widget %}
    {% spaceless %}

        {{ block('form_widget') }}
        {% if image_url is not null %}
            <img src="{{ image_url | imagine_filter('estate_admin_form') }}"/>
        {% endif %}

    {% endspaceless %}
{% endblock %}
